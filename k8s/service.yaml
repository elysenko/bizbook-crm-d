apiVersion: v1
kind: Service
metadata:
  name: bizbook-crm-d
  namespace: colossus-bizbook-crm-d
  labels:
    app: bizbook-crm-d
    managed-by: colossus
spec:
  type: ClusterIP
  selector:
    app: bizbook-crm-d
  ports:
    - port: 80
      targetPort: 8080
