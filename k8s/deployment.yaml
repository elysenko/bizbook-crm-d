apiVersion: apps/v1
kind: Deployment
metadata:
  name: bizbook-crm-d-staging
  namespace: colossus-bizbook-crm-d-staging
  labels:
    app: bizbook-crm-d-staging
    managed-by: colossus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bizbook-crm-d-staging
  template:
    metadata:
      labels:
        app: bizbook-crm-d-staging
    spec:
      hostAliases:
        - ip: "192.168.50.250"
          hostnames:
            - "ubuntu.desmana-truck.ts.net"
      containers:
        - name: app
          image: ubuntu:30500/bizbook-crm-d-staging:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: app-secrets
                optional: true
